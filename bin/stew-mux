#!/usr/bin/env bash
# Dispatch to a mux profile script

BIN_DIR="$HOME/dotfiles/bin"
profile="${1:-binsentry}"

script="$BIN_DIR/stew-mux-$profile"

if [ ! -x "$script" ]; then
    echo "stew-mux: unknown profile '$profile'" >&2
    exit 1
fi

exec "$script"
